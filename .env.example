# --- Backend (Django) ---
# Clave secreta para Django (genera una nueva usando: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())')
DJANGO_SECRET_KEY=django-insecure-#u&f@v8u=v$ko29w=3yr%u8bggt#d7vg@eu-f&jr9c4kbbfzyo

# Modo debug (1 para desarrollo, 0 para producción)
DJANGO_DEBUG=1

# URL de la base de datos (PostgreSQL, MySQL, etc.)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/datapulse_db

# Lista de hosts permitidos (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1,datapulse-ud95.onrender.com

# --- Frontend (Angular) ---
# URL base de la API para el frontend
API_URL=https://datapulse-ud95.onrender.com/api

# --- Configuración de CORS ---
# Dominios autorizados para acceder a tu API
CORS_ALLOWED_ORIGINS=http://localhost:4200,https://data-pulse-alpha.vercel.app

# --- Variables de almacenamiento/servicio (Opcional) ---
# Ejemplo: si usas servicios de terceros
EMAIL_HOST_USER=admin@datapulse.com
EMAIL_HOST_PASSWORD=Password123*